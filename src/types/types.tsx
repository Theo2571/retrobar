export {};

export interface PlaceData {
    placeName: string,
    eventName: string,
    eventDate: string,
    views: number,
    photographName: string,
    photos: string[],
}

export interface AdData {
    title: string,
    description: string,
}
